# Trae Auto Kit — CN

端口：`39241` / `39242` / `39243`（见 `config.json`）

| 操作 | 文件 |
|------|------|
| 检查端口 | `check-ports.bat` |
| 注入一次 | `一键启动.bat` |
| 常驻（推荐） | `常驻监视.bat` |
| 停止 | `停止.bat` |

点击逻辑见 `../shared/agent-click.js`。完整迁机说明见 `../交接说明.md`。
