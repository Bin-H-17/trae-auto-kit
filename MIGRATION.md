# 迁机清单（勾选）

完整说明见 **[`交接说明.md`](交接说明.md)**。

1. [ ] 安装 Node.js（`node -v`）
2. [ ] 解压/复制整个 `trae-auto-kit` 到新电脑
3. [ ] 编辑 `setup-shortcuts.ps1` 顶部路径（对照 `paths.example.json`）
4. [ ] 执行 `setup-shortcuts.ps1`
5. [ ] 结束旧 Trae 进程，用新快捷方式打开
6. [ ] `cn\check-ports.bat` / `work\check-ports.bat`
7. [ ] 启动对应 `常驻监视.bat`
8. [ ] 验收：运行 / 仍要运行 / 继续；问卷「下一个」不点

端口默认：CN `39241–39243`，Work `39341–39343`（见各目录 `config.json`）。
