# Trae Auto Kit — 交接说明（迁机必读）

本包是 **Trae CN + Trae Work** 的精简代点工具：自动点击会卡住 Agent 的确认按钮，无需装 VS Code 扩展、无需粘贴控制台。

**打包日期**：2026-07-31  
**源目录（本机）**：`D:\创意工坊\trae-auto-kit`  
**交付位置**：`E:\trae-auto-kit\` 与 `E:\trae-auto-kit.zip`

---

## 1. 简介

| 是 | 不是 |
|----|------|
| CDP 注入 + 常驻重注 | 官方插件 / vsix |
| CN 与 Work **同一套**点击逻辑 | 坐标鼠标点击 |
| 可整夹拷到新电脑 | 上游整仓搬运 |

**会自动点（优先级）**  
仍要运行 → **执行**（「确认执行」弹层）→ 确认运行/继续运行 → **继续** → 删除 → 全部接受 → 运行  
（出现「运行风险命令」弹层时，不点底层「运行」，只点「仍要运行」）

**默认不点**  
下一个、取消、保留（问卷/误点防护）

**面板**  
可拖标题栏、可点「－」收起；位置会记住。

**无法 100% 保证的情况**  
Trae 改版改 DOM/文案；新按钮需进白名单；官方硬拦且界面无按钮。

---

## 2. 目录结构（齐全但不冗杂）

```text
trae-auto-kit/
  交接说明.md          ← 本文件（迁机主文档）
  README.md            ← 日常怎么用（短）
  MIGRATION.md         ← 迁机清单（勾选用）
  setup-shortcuts.ps1  ← 一键重建桌面/开机快捷方式
  paths.example.json   ← 新电脑路径对照样例
  shared/
    agent-click.js     ← 唯一页内点击脚本（CN/Work 共用）
    cdp.mjs            ← CDP 注入/监视
    load-script.mjs    ← 按产品注入 CN|Work 标签
  cn/                  ← Trae CN（端口 39241–39243）
  work/                ← Trae Work（端口 39341–39343）
  vendor-notes/        ← MIT 归属说明（无上游整仓）
```

每个产品目录内日常文件：

| 文件 | 作用 |
|------|------|
| `一键启动.bat` | 检查端口并注入一次 |
| `常驻监视.bat` | 后台常驻，丢面板自动重注（推荐） |
| `停止.bat` | 停掉本产品的监视进程 |
| `check-ports.bat` | 看 CDP 端口是否 OK |
| `config.json` | 端口、监视间隔 |
| `probe-buttons.mjs`（仅 work） | 弹层出现时抓按钮文案/class |

**刻意不含**：上游 extension/vsix、旧粗点击器、粘贴 DevTools 教程、运行日志。

---

## 3. 新电脑安装（按顺序）

### 3.1 依赖

1. 安装 [Node.js LTS](https://nodejs.org/)，终端执行 `node -v` 能出版本。  
2. 安装好 **Trae CN**、**Trae Work（TRAE SOLO CN）**（路径可与本机不同）。

### 3.2 解压本包

任选其一：

- 解压 `E:\trae-auto-kit.zip` → 例如 `D:\创意工坊\trae-auto-kit\`  
- 或直接复制整个 `E:\trae-auto-kit\` 文件夹

### 3.3 改路径并重建快捷方式

1. 打开 `setup-shortcuts.ps1`，改顶部 `$Paths`（exe、user-data 目录）。可对照 `paths.example.json`。  
2. PowerShell：

```powershell
cd "你的\trae-auto-kit路径"
powershell -ExecutionPolicy Bypass -File .\setup-shortcuts.ps1
```

脚本会：

- 给桌面 Trae CN / Work 快捷方式加上调试端口  
- 创建桌面「常驻监视 / 停止」快捷方式  
- 写入开机启动（Startup）里的 CN + Work 监视

### 3.4 必须用新快捷方式重启 Trae

任务管理器结束旧的 Trae CN / TRAE SOLO CN，再用桌面新快捷方式打开。  
**旧进程没有调试端口，脚本连不上。**

### 3.5 验收

1. `cn\check-ports.bat` / `work\check-ports.bat` → 开着的实例端口显示 OK  
2. 双击 `cn\常驻监视.bat`、`work\常驻监视.bat`  
3. 窗口右上角出现 **Trae Auto Kit CN/Work** 面板  
4. 触发「运行 → 仍要运行」「继续」；日志出现 `clicked "..."`  
5. 问卷「下一个」不应被点  

---

## 4. 日常使用（熟手）

1. 只用带端口的快捷方式开 Trae（或开机后监视已在跑）。  
2. 开机启动已挂监视则一般不用再点；否则点桌面「TraeCN-常驻监视」「TraeWork-常驻监视」。  
3. 面板挡住界面 → **拖标题栏**，或点 **－** 收起。  
4. 不想自动删文件 → 取消面板上「删除」勾选。  
5. 出问题 → 先 `check-ports`，再 `一键启动`，仍不行看对应 `logs\`。

### 端口约定

| 快捷方式 | 参数要点 |
|----------|----------|
| Trae cn | `--remote-debugging-port=39241` |
| Trae_cn_2 | `--user-data-dir=...TraeData1` + `39242` |
| Trae_cn_3 | `--user-data-dir=...TraeData2` + `39243` |
| TRAE Work CN | `--remote-debugging-port=39341` |
| TRAE Work CN 2 | `...TraeWorkData1` + `39342` |
| TRAE Work CN 3 | `...TraeWorkData2` + `39343` |

端口写在 `cn/config.json`、`work/config.json`，改端口必须同步改快捷方式。

---

## 5. 官方设置建议（第一层）

脚本是**第二层**代点。建议在 Trae 内：

- CN / Work：**命令运行方式 → 自动运行**（或你信任的最高档）  
- Work 高危二次确认仍可能出「仍要运行」→ 靠本 kit 点掉  

问卷类仍建议人工；本 kit 默认不点「下一个 / 取消」。

---

## 6. 改版后按钮点不到时

1. 保持确认弹层开着  
2. Work：`node work\probe-buttons.mjs 39342`（端口按实际）  
3. 看 `work\logs\probe-*.json`  
4. 把新文案/class 补进 **`shared/agent-click.js`**（不要再复制一份 Work 脚本）  
5. 重新跑「常驻监视」或「一键启动」

---

## 7. 归属

按钮选择器参考 [luw2007/trae-auto-accept](https://github.com/luw2007/trae-auto-accept)（MIT）。  
「仍要运行」优先、风险弹层守卫、问卷黑名单、CN/Work 共用注入为本包本地增强。  
详见 `vendor-notes/`。

---

## 8. 迁机检查清单

- [ ] Node.js 已装  
- [ ] 整包已拷到新机  
- [ ] `setup-shortcuts.ps1` 路径已改并执行成功  
- [ ] Trae 已用**新快捷方式**重启  
- [ ] check-ports OK  
- [ ] 常驻监视在跑，面板可见可拖  
- [ ] 试过 运行 / 仍要运行 / 继续  
- [ ] 未再依赖旧目录 `trae-cn-auto-click` 或上游 vsix  

完成以上即可在新电脑独立使用。
